export interface Message {
    id? : number,
    name? : string,
    surname? : string,
    title? : string,
    messageContent? : string,
    emailAddress? : string,
    dateOfSend? : Date,
    isRead? : boolean
}
